# hacky-kernels

trying to map all exploit classes for [pygpubench](https://github.com/gpu-mode/pygpubench). looking for both kernels that bypass correctness but mainly kernels that can hack/game/bypass the timing so that they appear faster than they are so they could top gpu mode hackathons' leaderboards. 
